import { FC } from 'react';

const NotFoundPage: FC = () => {
    return (
        <div className="container">
            <h2 className="notFound">Not Found Page</h2>
        </div>
    );
};

export default NotFoundPage;
